<% event.participants.take(event.participants.count - 1).each do |p| %>
<%= link_to p.user.full_name, p.user %>,
<% end %>
<%= link_to event.participants.last.user.full_name, event.participants.last.user %>