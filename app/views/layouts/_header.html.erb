<%= link_to "Sportsfriender", root_path, :class => 'brand' %>
<ul class="nav pull-right">
  <% if user_signed_in? %>
	<li>
    <%= link_to 'My Profile', edit_user_path(current_user), :class => 'i_h_profile' %>
    </li>
    <li>
    <%= link_to t('logout'), destroy_user_session_path, :method=>'delete' %>
    </li>
  <% else %>
 	<li>
	 <%= button_to(t('signup'), new_user_registration_path, :class => 'btn btn-success', :method => :get) %>
	</li>
	<li>
	<%= button_to(t('login'), new_user_session_path, :class => 'btn btn-inverse', :method => :get) %>
	</li>
  <% end %>

  
</ul>
